import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.irg.lacocinadecarmen',
  appName: 'laCocinaDeCarmen',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
